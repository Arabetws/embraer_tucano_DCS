-- Este arquivo estava ausente nesta pasta. Adicionado apenas para declarar
-- o material do espelho ("t27_mirrors") como textura de reflexo dinâmico,
-- igual ao mod de referência t-27-community (luizrenault), que usa o mesmo
-- modelo de cockpit (cockpit_t-27.EDM, idêntico byte a byte) e tem os
-- espelhos funcionando.

livery =
{
	{ "t27_mirrors", 0, "mirrors", true }, -- dynamic mirror replacement texture
}

name = ""
